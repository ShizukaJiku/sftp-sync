<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.shizuka</groupId>
  <artifactId>sftp-sync</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>sftp-sync</name>
  <description>Sincronizador de carpetas multi-PC sobre SFTP, estilo Git</description>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <picocli.version>4.7.7</picocli.version>
    <jackson.version>2.21.3</jackson.version>
    <sshd.version>2.16.0</sshd.version>
    <bouncycastle.version>1.80</bouncycastle.version>
    <slf4j.version>2.0.16</slf4j.version>
    <junit.version>5.11.4</junit.version>
    <assertj.version>3.26.3</assertj.version>

    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
    <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
    <exec-maven-plugin.version>3.5.0</exec-maven-plugin.version>
    <native-maven-plugin.version>0.10.4</native-maven-plugin.version>

    <main.class>io.github.shizuka.sftpsync.Main</main.class>
  </properties>

  <dependencies>
    <!-- SSH/SFTP vía Apache MINA SSHD. Reemplaza sshj 0.40 que tiene
         issue #871 (incompatible con BC en native-image). MINA permite
         BC en native con el patrón rerun de chirontt/jgit.pgm.native. -->
    <dependency>
      <groupId>org.apache.sshd</groupId>
      <artifactId>sshd-core</artifactId>
      <version>${sshd.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.sshd</groupId>
      <artifactId>sshd-sftp</artifactId>
      <version>${sshd.version}</version>
    </dependency>

    <!-- BouncyCastle: provider JCE moderno. ChaCha20-Poly1305, curve25519,
         Ed25519. En native-image, BouncyCastleInitializer registra el
         provider en build-time vía Security.addProvider. MINA lo detecta
         vía SecurityUtils.isBouncyCastleRegistered. -->
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
      <version>${bouncycastle.version}</version>
    </dependency>

    <!-- CLI parsing -->
    <dependency>
      <groupId>info.picocli</groupId>
      <artifactId>picocli</artifactId>
      <version>${picocli.version}</version>
    </dependency>

    <!-- JSON (jackson-jr: GraalVM-friendly, sin reflection mágica) -->
    <dependency>
      <groupId>com.fasterxml.jackson.jr</groupId>
      <artifactId>jackson-jr-objects</artifactId>
      <version>${jackson.version}</version>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>${slf4j.version}</version>
    </dependency>

    <!-- Tests -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>${assertj.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven-compiler-plugin.version}</version>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>info.picocli</groupId>
              <artifactId>picocli-codegen</artifactId>
              <version>${picocli.version}</version>
            </path>
          </annotationProcessorPaths>
          <compilerArgs>
            <arg>-Aproject=${project.groupId}/${project.artifactId}</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${maven-surefire-plugin.version}</version>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>${exec-maven-plugin.version}</version>
        <configuration>
          <mainClass>${main.class}</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- Activar con: mvn -Pnative package -->
    <profile>
      <id>native</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.graalvm.buildtools</groupId>
            <artifactId>native-maven-plugin</artifactId>
            <version>${native-maven-plugin.version}</version>
            <extensions>true</extensions>
            <executions>
              <execution>
                <id>build-native</id>
                <goals>
                  <goal>compile-no-fork</goal>
                </goals>
                <phase>package</phase>
              </execution>
            </executions>
            <configuration>
              <imageName>sftp-sync</imageName>
              <mainClass>${main.class}</mainClass>
              <buildArgs>
                <buildArg>--no-fallback</buildArg>
                <buildArg>-H:+ReportExceptionStackTraces</buildArg>
                <!-- Solo locales en/es para mensajes del CLI. Ahorra ~1-2 MB. -->
                <buildArg>-H:IncludeLocales=en,es</buildArg>
                <!-- Optimización maxima de runtime. -O3 + AVX2 + BMI1/2 (x86-64-v3)
                     da ~30% más rápido en hash SHA-256 (medido localmente con
                     archivos de 50 MB: 373 ms → 258 ms). Requiere CPU Haswell
                     2013+ o Excavator 2015+ — toda PC desktop/laptop moderna. -->
                <buildArg>-O3</buildArg>
                <buildArg>-march=x86-64-v3</buildArg>

                <!-- ===== MINA SSHD + BouncyCastle en native-image ===== -->
                <!-- Patrón validado por chirontt/jgit.pgm.native:
                     1) build-time init para todo sshd, slf4j, bouncycastle y
                        nuestro BouncyCastleInitializer.
                     2) ClassInitialization con :rerun para las clases que tienen
                        SecureRandom o DRBG en su <clinit>. `rerun` significa
                        "ya inicializado en build-time, pero RE-inicializar en
                        run-time" — resuelve el conflicto de seeds congeladas.
                -->
                <buildArg>--initialize-at-build-time=org.apache.sshd,org.slf4j,org.bouncycastle,io.github.shizuka.sftpsync.nativesupport.BouncyCastleInitializer</buildArg>
                <buildArg>-H:ClassInitialization=org.apache.sshd.common.random.JceRandom:rerun,org.apache.sshd.common.random.JceRandom$Cache:rerun,org.bouncycastle.crypto.CryptoServicesRegistrar:rerun,org.bouncycastle.jcajce.provider.drbg.DRBG:rerun,org.bouncycastle.jcajce.provider.drbg.DRBG$Default:rerun,org.bouncycastle.jcajce.provider.drbg.DRBG$NonceAndIV:rerun</buildArg>
              </buildArgs>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
